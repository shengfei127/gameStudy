{
  "bsonType": "object",
  "required": ["clientId", "progress", "createdAt", "updatedAt"],
  "permission": {
    "read": false,
    "create": false,
    "update": false,
    "delete": false
  },
  "properties": {
    "_id": {
      "description": "uniCloud 自动生成的记录 ID"
    },
    "clientId": {
      "bsonType": "string",
      "description": "学习档案所有者标识，登录后使用账号用户 ID，未登录兼容匿名客户端 ID"
    },
    "progress": {
      "bsonType": "object",
      "description": "宠物、积分、成长值、学习记录和喂养记录快照"
    },
    "createdAt": {
      "bsonType": "string"
    },
    "updatedAt": {
      "bsonType": "string"
    }
  }
}
